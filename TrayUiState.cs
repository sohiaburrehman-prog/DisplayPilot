namespace PrimaryDisplaySwap;

/// <summary>Cross-UI flags shared between the WPF panel and WinForms tray menu.</summary>
internal static class TrayUiState
{
    public static bool TrayMenuOpen { get; set; }
}
